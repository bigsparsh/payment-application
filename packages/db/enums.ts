export enum TransactionStatus {
  Pending = "PENDING",
  Completed = "COMPLETED",
  Failed = "FAILED",
}

export enum AuthProvider {
  Credentials = "CREDENTIALS",
  Google = "GOOGLE",
  Github = "GITHUB",
}
