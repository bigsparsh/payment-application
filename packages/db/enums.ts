export enum AuthProvider {
  GOOGLE = "GOOGLE",
  CREDENTIALS = "CREDENTIALS",
  GITHUB = "GITHUB",
}
export enum TransactionStatus {
  PENDING = "PENDING",
  SUCCESS = "SUCCESS",
  FAILED = "FAILED",
}
export enum Bank {
  AXIS = "AXIS",
  HDFC = "HDFC",
  SBI = "SBI",
}
